# `.agents` 协作资产

此目录将项目知识从聊天上下文中移到版本控制中，供人和 AI 共同维护。

```text
.agents/
├─ plans/       # 已确认的架构基线与分阶段改造计划
├─ skills/      # 项目特定的领域规则和实现边界
└─ workflows/   # 可重复执行的任务流程与验收条件
```

使用规则：先读根目录 `AGENTS.md`，然后按任务读取一个 workflow；只有任务涉及对应领域时才读取 skill。计划是路线图，不是自动授权执行全部重构。
