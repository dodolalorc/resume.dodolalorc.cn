# 主题与排版流程

1. 判断是主题配置变更、共享卡片样式变更，还是布局编排变更；避免把主题专属判断扩散到每个组件。
2. 新主题通过 `bun run theme:new -- "Theme Name"` 创建，再补全配置和预览资源。
3. 核对 sectionOrder、sidebarSections 与实际可用编辑区块一致。
4. 在最少、典型、超长三组数据下检查屏幕和打印；重点看中文换行、长链接、图片与分页。
5. 更新主题注册测试和 README 的主题说明（如用户可见能力发生变化）。
