# 架构基线（2026-07-16）

## 当前链路

```text
cv.json → resume-view.vue 的多个 ref → 编辑抽屉 / 预览卡片
                                    ├→ localStorage（设置项）
                                    └→ export-resume.html + iframe 打印
themes/*.ts → 主题注册表 → resume-view.vue → CSS 变量 / 卡片
```

## 已确认的问题

| 优先级 | 问题 | 证据 | 风险 |
| --- | --- | --- | --- |
| 已处理 | 运行时导入没有结构校验 | 已由 `src/utils/resume-import.ts` 统一进行大小、协议与结构校验 | 畸形 JSON 不会进入状态 |
| 已处理 | 状态模型分裂 | 已移除未接入的 Pinia store，页面状态成为唯一事实来源 | 后续阶段再将页面状态拆至 composable |
| 部分处理 | 核心页面过大 | 导入流程已提取到 `resume-import-dialog.vue`，主页面约 1,793 行，仍承担设置与画布编排 | 高冲突风险已降低，仍应按路线图继续拆分 |
| 已处理 | 持久化散落且键无统一版本 | 设置项已收敛至版本化的 `resume-preferences-v2` 单一键 | 旧版设置不会自动迁移，作为有意的安全重置 |
| 部分处理 | 自动化测试覆盖薄弱 | 已新增导入解析、协议和科研分支测试（共 9 条） | 编辑、持久化、导出、主题切换仍需回归保护 |
| 已处理 | 国际化与产品行为不一致 | i18n 已收敛为唯一的 `zhHans` 文案包 | 当前产品行为与运行时配置一致 |
| 部分处理 | 类型命名与模型表达不清 | `research` 已改为非递归 `ResearchResumeConfig`；历史公开类型名待独立兼容迁移 | 公共契约仍需渐进清理 |

这份基线用于指导重构优先级；它不替代每次改动的测试与验收。
